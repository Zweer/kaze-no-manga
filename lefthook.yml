pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,json}"
      run: npx biome check --fix {staged_files}
      stage_fixed: true
    test:
      run: npx vitest run --passWithNoTests
